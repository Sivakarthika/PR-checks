name: PR Governance

on:
  pull_request:
    types: [opened]

jobs:
  assign-reviewer:
    runs-on: ubuntu-latest

    steps:

      # ✅ Step 1: Set token + reviewer
      - name: Set config
        run: |
          if [ "${{ github.base_ref }}" = "dev" ]; then
            echo "TOKEN=${{ secrets.DEV_TOKEN }}" >> $GITHUB_ENV
            echo "REVIEWER=skhastha" >> $GITHUB_ENV

          elif [ "${{ github.base_ref }}" = "qa" ]; then
            echo "TOKEN=${{ secrets.QA_TOKEN }}" >> $GITHUB_ENV
            echo "REVIEWER=skhastha" >> $GITHUB_ENV

          elif [ "${{ github.base_ref }}" = "release" ]; then
            echo "TOKEN=${{ secrets.RELEASE_TOKEN }}" >> $GITHUB_ENV
            echo "REVIEWER=Sivakarthika" >> $GITHUB_ENV

          else
            echo "No config found"
            exit 0
          fi

      # ✅ Step 2: Use token (NO env:, NO ${{ env.TOKEN }})
      - name: Assign reviewer
        run: |
          echo "Reviewer: $REVIEWER"

          # ✅ Set GH_TOKEN at runtime (correct way)
          export GH_TOKEN="$TOKEN"

          # Remove existing reviewer (ignore error)
          gh api --method DELETE \
            repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/requested_reviewers \
            -F reviewers[]="$REVIEWER" || true

          sleep 2

          # Add reviewer (triggers email)
          gh api --method POST \
            repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }}/requested_reviewers \
            -F reviewers[]="$REVIEWER"
